import { useEffect } from "react";
import { MAIN_URL, PORT } from "../../../constants/constants";

export default function DuplShipsTicket() {
  useEffect(() => {
    // const fetchData = async () => {
    //   const request = await fetch(MAIN_URL + PORT +)
    // }
  }, []);
  return <div>Заявление</div>;
}
